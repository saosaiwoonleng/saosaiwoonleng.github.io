# Maintenance Report — 2026-09-07

## Repository Summary

- **Repository:** saosaiwoonleng/saosaiwoonleng.github.io
- **Date:** 2026-09-07
- **Scope:** Routine scheduled full-repository health check — static personal portfolio site (GitHub Pages, Jekyll theme wrapper, no application backend, no package manager, no database).

## Note on repository state

`main` had zero commits since the last audit (`2930dca`, 2026-09-03) — no site content, config, or CI changed in the interim. No open PRs at the start of this run. This is a re-verification pass, not a change-driven one.

Re-examined the 14 backlogged `claude/*` remote branches flagged in the 2026-09-03 report (no open PRs against any of them, still unmerged). Diffed each against current `main` to classify rather than just re-list them:

- **13 of 14** (`gallant-volta-{0os2gt,as97pq,fl3tko,o26d50,ornnyr,ovhvif,twyyr1,wshxu9}`, `happy-bardeen-{656j15,ofymh8,pja5d2,ql26l7,zjaxp5}`) carry genuine divergent site/script changes (`index.html`, and in some cases `scripts/check-links.js` or `scripts/check-local-links.js`, vendored library files). Spot-checking one (`gallant-volta-fl3tko`, branched mid-June) showed it would **downgrade jQuery from 3.7.1 to 3.7.0** and reintroduce a full unminified duplicate `Content/bootstrap.css`/`bootstrap-grid.css`/`bootstrap-reboot.css` set alongside a competing `.github/workflows/ci.yml` that conflicts with the `html-check.yml`/`link-check.yml` split already on `main` — i.e. merging it now would be a regression, not an update. All 13 predate the README/CI additions that later landed on `main` through different, already-merged PRs, so their "add README, add CI, patch jQuery" work is superseded by what's already in place, just via a different implementation. Not merged, consistent with prior audits — per-branch review to confirm each is safe to discard (rather than assumed) is still out of scope for a routine pass, but the regression risk above is now documented so a future pass (or the operator) doesn't need to re-derive it.
- **1 of 14** (`happy-bardeen-4kmpl6`) contains only maintenance-report and merge commits; diffed against current `main` it produces **zero net change outside of already-superseded `docs/maintenance-report/*.md` files** (those reports were later re-added on `main` in their current form). This branch is redundant with no unique content — flagged as a safe-delete candidate for the next run rather than deleted in this one, to keep this run's actions limited to what was fully verified before the report was written.

No action taken on branch backlog this run beyond this classification (documented, not executed, per the "per-branch review before touching unlanded work" rule).

## Dependency Audit

No package manager or lock file; third-party libraries remain vendored directly under `Content/`. Re-checked both versions in place against current advisories:

| Library | Version in repo | Latest available | Known CVEs |
| --- | --- | --- | --- |
| jQuery | 3.7.1 (`Content/Hola/js/jquery-3.7.1.min.js`) | 4.0.0 (major) | No confirmed CVEs outstanding against 3.7.1 found this run; jQuery 3.x remains critical-fix-only maintenance upstream |
| Bootstrap | 4.6.0 (CSS only — `Content/bootstrap.min.css`, no Bootstrap JS shipped) | 5.x | Only current Bootstrap 4 issue found is CVE-2024-6531 (Carousel JS XSS via unsanitized `href`, later withdrawn by the Bootstrap project as outside their security model) — JS-only, doesn't apply since this site consumes CSS only |
| AOS, Modernizr, Pace.js, Chaffle/MovingLetter | vendored, unversioned | n/a | No known CVEs; static-effect libraries, unchanged since last audit |

No dependency files changed since the last audit (nothing changed on `main` at all). jQuery 4.0.0 and Bootstrap 5 remain deliberately deferred — no security driver, both are non-trivial breaking upgrades.

## Security Review

- Targeted secret scan (`api[_-]?key`, `secret`, `password`, `private[_-]?key`, `aws_access`, PEM `BEGIN ... PRIVATE KEY` markers) over `index.html`, `404.html`, `_config.yml`, `README.md`, and vendored CSS/JS: only hits are the door-lock hobby-project "password" keypad prose in `index.html` and CSS `input[type="password"]` selectors — same known false positives reviewed every prior audit.
- No stray/debug files (`*.log`, `*.tmp`, `*.bak`, `*.swp`, `.DS_Store`) in the working tree.
- `_config.yml` unchanged — theme setting only, no debug flags or unsafe defaults.
- No secrets or credentials found.

No security issues found requiring action.

## Code Quality

No code changed since the last audit; nothing new to review. No refactoring performed.

## Testing

Static site, no application logic, no unit/integration test suite applicable. Ran the actual CI commands locally against the working tree (portable JRE + `lychee` release binary fetched fresh into `/tmp`, matching the prior pattern — nothing installed system-wide or committed):

- `html5validator --root . --match "*.html" --ignore-re "Content/" --ignore "Element \"ul\" not allowed as child of element \"span\""` — **pass**, no output/errors.
- `lychee --no-progress --root-dir . --exclude '^https://formspree\.io/' --exclude '^https://(www\.)?reddit\.com/' index.html 404.html` — **pass**, 95 links checked, 70 unique, 89 OK, 0 errors, 6 excluded, 2 redirects (non-failing), identical to the 2026-09-03 results since site content hasn't changed.

## CI/CD

No changes made to CI configuration this run; `.github/workflows/link-check.yml`, `.github/workflows/html-check.yml`, and GitHub's default CodeQL setup continue to function as intended. HTML Validation remains path-filtered to `**.html` changes, so it won't fire on this report-only commit — verified equivalent locally instead (see Testing).

## Documentation

README.md reviewed against current implementation — no drift since the last review (nothing on `main` changed), still accurate. No updates required.

## Release Status

- No code/config changes required this run — pure re-verification with a documented branch-backlog classification.
- `main` is deployment-ready; GitHub Pages continues serving the current state successfully.
- Backlog carried forward: 13 branches with genuine (and in at least one case regressive) divergent changes, 1 branch (`happy-bardeen-4kmpl6`) now confirmed as a safe-delete candidate for a future run.

## Risk Assessment

- This run's own change (this report): **Low** — documentation only, no code/config impact.
- No merges or deletions performed this run: **N/A** — branch backlog classified but left untouched, so there is no rollback surface to document.

## Status

All dependencies are up to date. No changes required beyond this audit report and the branch-backlog classification above.
