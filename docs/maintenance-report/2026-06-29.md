# Maintenance Report — 2026-06-29

## Repository Summary

- **Repository:** saosaiwoonleng/saosaiwoonleng.github.io
- **Date:** 2026-06-29
- **Scope:** Full repository inspection, dependency/security audit, repository hygiene, documentation, and CI/CD bootstrap.
- **Project type:** Static personal portfolio site hosted on GitHub Pages (Jekyll theme `jekyll-theme-midnight`). No backend, database, package manager, or build pipeline.

## Dependency Audit

No package manager is used; all third-party libraries are vendored as static files under `Content/` or loaded from a public CDN on the 404 page.

| Dependency | Current | Latest | Update Type | Notes |
| --- | --- | --- | --- | --- |
| jQuery (vendored) | 3.7.0 | 3.7.1 | Patch | 3.7.1 only fixes a non-security `<tr>` dimension regression and restores an accidentally removed Sizzle method. No action required. |
| Bootstrap CSS (404.html, CDN) | 3.3.7 | 3.4.1 (last 3.x) | Patch/security | 3.3.7 predates several Bootstrap 3.x XSS fixes. Bumped the CDN reference to 3.4.1. Page only loads Bootstrap's CSS, not its JS, so the underlying tooltip/popover/scrollspy XSS advisories (which live in the JS plugins) were never exploitable here. |
| Bootstrap 4.6.0 (vendored, `Content/*.css`) | 4.6.0 | — | Removed | Dead code — not referenced by any HTML/CSS in the repo. Deleted instead of upgraded (see Security Review). |
| Modernizr, Pace.js, AOS, Chaffle, Moving Letter | pinned, various | — | — | No CVE databases track these niche front-end libraries; they don't parse remote/untrusted input, so risk at pinned versions is low. No action taken. |

## Security Review

| Finding | Severity | Action Taken |
| --- | --- | --- |
| Dead vendored Bootstrap 4.6.0 CSS (12 files) carried a known CVE class (CVE-2024-6531, carousel XSS, CVSS 6.4) in its upstream JS — unused in this repo but still unnecessary attack surface and repo bloat. | Low (unused) | Removed entirely. |
| `404.html` pinned Bootstrap CSS to EOL 3.3.7 from a third-party CDN without SRI. | Low | Bumped to 3.4.1. (Could not add a Subresource Integrity hash — outbound fetch to the CDN was blocked in this environment, and a fabricated hash would break the stylesheet. Recommend adding `integrity`/`crossorigin` attributes manually, or vendoring this file locally like the rest of the site's assets, in a follow-up.) |
| `debug.log` (Chrome browser internal settings-version error log) committed to repo root since 2021. | Informational | Removed; not a secret, just noise. |
| `.vscode/launch.json` tracked in Git despite being listed in `.gitignore`. | Informational | Untracked via `git rm --cached` (file is preserved locally, just no longer versioned), matching the repo's own declared intent. |
| Contact/visitor-alert forms post to Formspree using a public form ID embedded in the HTML. | None | This is the intended Formspree integration model — the form ID is not a secret. No action needed. |
| Secret/credential scan of HTML/JS for API keys, tokens, passwords. | None found | No exposed secrets. Matches in project description text ("password", "EPROM") are content describing an embedded-systems portfolio project, not real credentials. |

No authentication, authorization, session handling, or server-side code exists in this repository, so those review areas are not applicable.

## Code Quality

- Removed 12 unused vendored Bootstrap 4.6.0 files (dead code).
- No duplicate logic or other refactoring opportunities identified that would justify touching working markup/JS; avoided style-only changes per scope guidance.

## Testing

- No unit/integration test suite exists or is applicable to a static HTML/CSS/JS site with no build step.
- Added an automated link/asset check (see CI/CD) as the closest equivalent regression guard for this project type.

## CI/CD

- **Before:** no `.github/workflows`, no automation of any kind.
- **Added:** `.github/workflows/link-check.yml` — runs `lychee` against `index.html` and `404.html` on every push and pull request targeting `main`, failing the run on any broken internal or external link/asset reference.
- GitHub Pages itself continues to build and deploy directly from the `main` branch root via Jekyll; this workflow does not change that deployment path.

## Documentation

- Added `README.md` (none existed previously): project overview, technology stack, project structure, local development setup, build/test instructions, CI/CD description, and deployment/rollback process.
- Added this maintenance report.

## Release Status

- Changes are pushed to branch `claude/gallant-volta-4xegnx` and submitted as a pull request for review rather than merged directly into `main` — direct-to-main merges are outside this session's permitted workflow.
- No build step, so "deployment readiness" reduces to: HTML still references only valid local paths (verified manually after asset removal) and the new CI workflow is in place to catch regressions going forward.
- Remaining recommended follow-ups (not done in this pass, to keep changes minimal/reversible):
  - Add an SRI `integrity` hash to the 404 page's Bootstrap CDN `<link>`, or vendor that file locally for consistency with the rest of the site.
  - Consider adding `robots.txt`/`sitemap.xml` for SEO if desired.
  - Consider Dependabot for the new GitHub Actions workflow (`github-actions` ecosystem) to keep `lychee-action` current.
