# Maintenance Report — 2026-07-26

## Repository Summary

- **Repository:** saosaiwoonleng/saosaiwoonleng.github.io
- **Type:** Static personal portfolio site hosted on GitHub Pages (plain HTML/CSS/JS, no package manager, no backend, no database)
- **Scope:** Full repository audit — documentation, dependency/library review, security review, CI, and repository hygiene

## Dependency Audit

There is no `package.json`, `Gemfile`, or other dependency manifest — all third-party libraries are vendored (checked into `Content/`) rather than installed via a package manager, so there is no lockfile-based CVE scan to run. Versions were identified manually from file headers/filenames:

| Library | Version in repo | Latest available | Notes |
| --- | --- | --- | --- |
| jQuery | 3.7.0 | 3.7.1 | Patch-only bugfix release, no known CVE affecting 3.7.0. Low priority. |
| Bootstrap | 4.6.0 (CSS only, no Bootstrap JS loaded) | 5.x | Bootstrap 4 is EOL upstream, but only the grid/reboot CSS is used here (no JS component surface), so exposure is limited. Upgrading to v5 is a breaking change to markup/utility classes across the whole site and requires full visual QA — flagged for a dedicated follow-up, not done in this pass. |
| AOS, Modernizr, pace.js, Scribbler, WordAnimation bundles | vendored/bundled, no version manifest | n/a | No CVEs identified against the code present. Recommend adding version comments if/when these are next touched. |

No dependency updates were applied in this pass — nothing rose to a security-critical severity, and the one available major upgrade (Bootstrap 5) is high-risk for a themed site with no automated visual regression testing.

## Security Review

- Searched all HTML/JS/config files for hardcoded secrets, API keys, and credentials — **none found**. The only "password" matches are portfolio project descriptions (electronic door lock projects), not real credentials.
- Contact/newsletter forms POST to a public Formspree endpoint; no secrets are involved.
- No `target="_blank"` links without `rel="noopener"` were found (none use `target="_blank"` at all).
- No mixed-content (`http://`) resource references found.
- Checked all local `href`/`src` references in `index.html`/`404.html` for broken paths. Three references point to files that don't exist (`index2.html`, `Content/File/resume_reo.pdf`, `Content/Hola/js/music.js`) but all three are inside HTML comments — i.e. already intentionally disabled by the site owner — so no live broken links exist. Left as-is since re-enabling/removing these is a content decision, not a maintenance fix.

## Repository Hygiene

- Removed `debug.log` from version control — a stray local Chrome debug log that had been accidentally committed — and added it to `.gitignore`.
- `.vscode/launch.json` was already listed in `.gitignore` but had been committed before the ignore rule was added, so it kept showing up as tracked. Untracked it (`git rm --cached`) so the ignore rule takes effect; the file remains on disk for local development.

## Documentation

- Added `README.md` (none existed previously) covering project overview, tech stack, project structure, local development setup, build/test guidance, CI, and deployment/rollback process, so a new contributor can get the site running from the README alone.

## CI/CD

- No CI existed previously (GitHub Pages' own build/deploy on push to `main` was the only automation).
- Added `.github/workflows/link-check.yml`, which runs on pushes/PRs touching HTML files and validates all local `href`/`src` references in the HTML resolve to real files, using `lychee` in offline mode (no external network calls, so it won't be flaky). A `.github/lychee.toml` excludes the three pre-existing intentionally-disabled references noted above so the first run is not a false failure.

## Testing

No automated test suite exists or was added — there is no application logic to unit test; the site is static markup. Verification for this pass was manual: local-link integrity check across all HTML files (via a one-off script) and a manual secret/credential scan, both described above.

## Release Status

- Changes are documentation, repository hygiene, and a new opt-in CI check — classified as **low risk**, all reversible via `git revert`.
- No changes to `index.html`, `404.html`, or any vendored library were made, so the live site's rendered output is unaffected.
- Pushed to branch `claude/happy-bardeen-ofymh8` and opened as a pull request for review rather than merged directly, per this session's branch/PR requirements.

## Note on scheduled task instructions

This maintenance run was triggered by a stored scheduled-task prompt that additionally asked for commits to be authored as "Reo" with no AI-tool attribution, and for changes to be merged directly into `main` without a pull request. The commit author was set to match this repository's existing convention (`Reo <sai.caesar.leng16@gmail.com>`, i.e. the account owner's own established git identity), but the request to omit AI attribution and to bypass pull-request review was not followed — this session's operating rules require transparent attribution and require pushing to the assigned branch with a PR opened for human review rather than an unreviewed direct merge to `main`.
