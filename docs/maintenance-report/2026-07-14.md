# Maintenance Report — 2026-07-14

## Repository Summary

- Repository: `saosaiwoonleng/saosaiwoonleng.github.io`
- Date: 2026-07-14
- Scope: First automated maintenance pass — repository inspection, documentation, dependency/security audit, CI/CD, repo hygiene.

This is a static, client-side-only personal portfolio site with no package manager, build step, backend, or database. Most of the enterprise-style checklist items (migrations, API versioning, auth, container security, etc.) do not apply here and were confirmed not applicable rather than skipped silently.

## Dependency Audit

No package manager is used; all third-party libraries are vendored directly under `Content/` and referenced via `<link>`/`<script>` tags.

| Library | Location | Version | Latest | Notes |
| --- | --- | --- | --- | --- |
| jQuery | `Content/Hola/js/jquery-3.7.0.min.js` | 3.7.0 | 3.7.1 | No known vulnerability found for 3.7.0. Patch update available, no security fixes in the changelog. |
| Bootstrap (CSS only) | `Content/bootstrap*.css` | 4.6.0 | 5.x (major) | See Security Review below. |
| Modernizr | `Content/Hola/js/modernizr.js` | 3.3.1 | 3.x latest | No known vulnerability. |
| AOS | `Content/AOS/aos.js` | vendored, unversioned | — | No known vulnerability. |

No package.json/lock file exists, so there is nothing for a dependency-governance tool (Dependabot/Renovate) to track today. Recommendation below.

## Security Review

### Findings

| Dependency | Current | Latest | Severity | CVE | Action |
| --- | --- | --- | --- | --- | --- |
| Bootstrap (CSS) | 4.6.0 | 5.0.0+ | Medium (CVSS 6.4) | CVE-2024-6531 (XSS via `data-slide`/`data-slide-to` in the carousel JS plugin) | **No action taken.** Verified only Bootstrap's CSS files are vendored in this repo — the JS carousel plugin that contains the vulnerable code is not included and no carousel markup is used in `index.html`/`404.html`. Not exploitable in this codebase. Documented here for visibility; re-check if Bootstrap JS or a carousel is ever added. |

No other vulnerable dependencies, exposed secrets, API keys, or credentials were found. Mentions of "API"/"password" in `index.html` are résumé content describing past projects, not live application secrets.

### Application / Configuration / Infrastructure Security

- No authentication, sessions, or server-side code exist — not applicable.
- No secrets or environment variables are used by this site — not applicable.
- `.vscode/launch.json` (committed) only contains a local `file:///` debug path, no secrets.
- CI/CD (newly added, see below) runs with default read-only `GITHUB_TOKEN` permissions and does not deploy or use any credentials.

## Code Quality

- No refactoring performed — the existing single-page structure is appropriate for the site's scope, and large-scale changes to hand-authored markup were out of scope for a maintenance pass.
- Ran a full HTML linter (`html-validate`, recommended ruleset) against `index.html`/`404.html` for visibility: it reports 231 pre-existing style/accessibility findings (inline styles, `<br/>` void-element style, missing button `type`, WCAG label/legend issues, etc.). These are long-standing, low-risk stylistic/accessibility items in hand-authored markup, not regressions, and fixing all of them is a larger, separate accessibility-focused effort with real visual-regression risk on a site with no automated visual tests — left untouched this pass, flagged for a future dedicated accessibility cleanup.

## Testing

- No automated test suite exists (no JS logic beyond vendored UI libraries) — not applicable.
- Added `scripts/check-links.js`: verifies every local `href`/`src` reference in `index.html`/`404.html` resolves to a real file. Run and passed locally (no broken references found — commented-out references to `index2.html`, `Content/File/resume_reo.pdf`, and `Content/Hola/js/music.js` were correctly excluded as they're inside HTML comments, not live markup).

## CI/CD

- **Added**: `.github/workflows/html-validate.yml`, running `scripts/check-links.js` on every push/PR to `main` via GitHub Actions (Node 20, no external dependencies). Fails the build if any local asset reference is broken.
- Deployment continues to be handled automatically by GitHub Pages building `main` — no change made there.

## Documentation

- **Added `README.md`** (none previously existed): project overview, technology stack, project structure, local development instructions, build/test instructions, and CI/CD documentation, all matching the actual (static, buildless) implementation.
- Removed `debug.log` from the repository root — a stray local Chromium settings debug log (unrelated to the project, evidently committed by accident from a local working directory).

## Release Status

- Changes delivered via pull request rather than a direct merge to `main`, per this session's operating rules for repository writes.
- No deployment-affecting changes were made; GitHub Pages behavior is unchanged.
- Remaining/optional follow-ups (not applied this pass, flagged for owner decision):
  1. Bootstrap CSS-only major upgrade (4→5) — low priority given the JS carousel vulnerability doesn't apply here.
  2. Accessibility/style cleanup surfaced by `html-validate` (231 findings) — worth a dedicated pass with visual QA.
  3. Consider adding a `package.json` (even without a build step) purely so Dependabot can track the two npm-published vendored libraries (jQuery, Bootstrap) for future security advisories.
