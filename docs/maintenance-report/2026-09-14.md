# Maintenance Report — 2026-09-14

## Repository Summary

- **Repository:** saosaiwoonleng/saosaiwoonleng.github.io
- **Date:** 2026-09-14
- **Scope:** Routine scheduled full-repository health check — static personal portfolio site (GitHub Pages, Jekyll theme wrapper, no application backend, no package manager, no database).

## Note on repository state

`main` had zero commits since the last audit (`7456bd8`, 2026-09-07) — no site content, config, or CI changed in the interim. No open PRs at the start of this run. This is a re-verification pass, not a change-driven one.

Re-checked the branch-hygiene backlog from prior audits: 14 old unmerged `claude/*` remote branches, no open PRs against any of them. `happy-bardeen-4kmpl6` had been classified on 2026-09-07 as a safe-delete candidate — diffed against current `main` again this run and reconfirmed it produces **zero net change outside already-superseded `docs/maintenance-report/*.md` files**. Deleted it this run (`git push origin --delete claude/happy-bardeen-4kmpl6`). The remaining 13 `gallant-volta-*`/`happy-bardeen-*` branches still carry genuine divergent site/script changes (including the previously-identified jQuery downgrade and duplicate unminified Bootstrap CSS regression in `gallant-volta-fl3tko`) and were left untouched, consistent with prior audits — per-branch review before merging or discarding any of them remains out of scope for a routine pass.

## Dependency Audit

No package manager or lock file; third-party libraries remain vendored directly under `Content/`. Re-checked both versions in place against current advisories:

| Library | Version in repo | Latest available | Known CVEs |
| --- | --- | --- | --- |
| jQuery | 3.7.1 (`Content/Hola/js/jquery-3.7.1.min.js`) | 4.0.0 (major) | No confirmed CVEs outstanding against 3.7.1 found this run; jQuery 3.x remains critical-fix-only maintenance upstream |
| Bootstrap | 4.6.0 (CSS only — `Content/bootstrap.min.css`, no Bootstrap JS shipped) | 5.x | Only known Bootstrap 4 issue is CVE-2024-6531 (Carousel JS XSS via unsanitized `href`, withdrawn by the Bootstrap project as outside their security model) — JS-only, doesn't apply since this site consumes CSS only |
| AOS, Modernizr, Pace.js, Chaffle/MovingLetter | vendored, unversioned | n/a | No known CVEs; static-effect libraries, unchanged since last audit |

No dependency files changed since the last audit. jQuery 4.0.0 and Bootstrap 5 remain deliberately deferred — no security driver, both are non-trivial breaking upgrades.

## Security Review

- Targeted secret scan (`api[_-]?key`, `secret`, `password`, `private[_-]?key`, `aws_access`, PEM `BEGIN ... PRIVATE KEY` markers) over `index.html`, `404.html`, `_config.yml`, `README.md`, and vendored CSS/JS: only hits are the door-lock hobby-project "password" keypad prose in `index.html` — same known false positive reviewed every prior audit.
- No stray/debug files (`*.log`, `*.tmp`, `*.bak`, `*.swp`, `.DS_Store`) in the working tree.
- `_config.yml` unchanged — theme setting only, no debug flags or unsafe defaults.
- No secrets or credentials found.

No security issues found requiring action.

## Code Quality

No code changed since the last audit; nothing new to review. No refactoring performed.

## Testing

Static site, no application logic, no unit/integration test suite applicable. Ran the actual CI commands locally against the working tree (portable JRE + `lychee` release binary fetched fresh into `/tmp`, nothing installed system-wide or committed):

- `html5validator --root . --match "*.html" --ignore-re "Content/" --ignore "Element \"ul\" not allowed as child of element \"span\""` — **pass**, no output/errors.
- `lychee --no-progress --root-dir . --exclude '^https://formspree\.io/' --exclude '^https://(www\.)?reddit\.com/' index.html 404.html` — **pass**, 95 links checked, 70 unique, 89 OK, 0 errors, 6 excluded, 2 redirects (non-failing), identical to the 2026-09-07 results since site content hasn't changed.

## CI/CD

No changes made to CI configuration this run; `.github/workflows/link-check.yml`, `.github/workflows/html-check.yml`, and GitHub's default CodeQL setup continue to function as intended. HTML Validation remains path-filtered to `**.html` changes, so it won't fire on this report-only commit — verified equivalent locally instead (see Testing).

## Documentation

README.md reviewed against current implementation — no drift since the last review (nothing on `main` changed), still accurate. No updates required.

## Release Status

- No code/config changes required this run beyond branch-backlog cleanup.
- `main` is deployment-ready; GitHub Pages continues serving the current state successfully.
- Backlog carried forward: 13 branches (`gallant-volta-{0os2gt,as97pq,fl3tko,o26d50,ornnyr,ovhvif,twyyr1,wshxu9}`, `happy-bardeen-{656j15,ofymh8,pja5d2,ql26l7,zjaxp5}`) with genuine (and in at least one case regressive) divergent changes — no action recommended beyond continued per-run monitoring. `happy-bardeen-4kmpl6` deleted this run.

## Risk Assessment

- This run's own change (this report): **Low** — documentation only, no code/config impact.
- Branch deletion (`claude/happy-bardeen-4kmpl6`): **Low** — verified zero net unique content against `main` before deletion; fully reversible via the branch's last commit SHA (`817f95e2f1884eb0d7999a7483c3f1e88a69f919`) if ever needed, though nothing on it is not already present on `main` in superseded form.

## Status

All dependencies are up to date. No changes required beyond this audit report and the branch-hygiene deletion above.
