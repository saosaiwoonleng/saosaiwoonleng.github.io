# Maintenance Report — 2026-06-24

## Repository Summary

- Repository: `saosaiwoonleng.github.io`
- Date: 2026-06-24
- Scope: General maintenance pass — repository inspection, dependency audit,
  security review, CI/CD, documentation. This is a static personal portfolio
  site (HTML/CSS/JS, GitHub Pages + Jekyll theme), with no backend, database,
  authentication, or package manager — several standard checklist areas
  (API compatibility, migrations, auth, runtime upgrades) are not applicable.

## Dependency Audit

No package manager is used; third-party libraries are vendored directly
under `Content/`.

| Library | Previous | Latest checked | Action |
| --- | --- | --- | --- |
| jQuery | 3.7.0 | 3.7.1 | Updated (patch, no breaking changes) |
| AOS, Modernizr, Pace.js, Chaffle, MovingLetter | unpinned (no version metadata in files) | n/a | No known CVEs; left as-is |

jQuery 3.7.1 fixes a `<tr>` dimension regression (notably affecting pages
using Bootstrap-style CSS), restores an internal `jQuery.find.tokenize`
method accidentally dropped in 3.7.0, and fixes `.hover()` and
`.text()`/HTMLDocument edge cases. No known CVEs were found for any vendored
library at the versions in use.

Replaced `Content/Hola/js/jquery-3.7.0.min.js` with
`Content/Hola/js/jquery-3.7.1.min.js` (official build, sourced via the
`jquery` npm package) and updated the `<script>` reference in `index.html`.

## Security Review

- No secrets, API keys, or credentials found in any tracked file.
- No mixed content (`http://`) references; all assets are same-origin or
  served over HTTPS.
- Contact form posts to Formspree (third-party form backend) — the
  form ID present in the markup is a public form identifier, not a secret.
- **Fixed:** `debug.log` (a local browser/Chromium settings debug log,
  unrelated to the project) was accidentally committed to the repository.
  Removed from tracking and added to `.gitignore`.
- **Fixed:** `.vscode/launch.json` was listed in `.gitignore` but had
  already been committed previously, leaking a local Windows username and
  folder path (`C:\Users\Reo\...`) in version control. Untracked it
  (`git rm --cached`) — the file remains on disk for local VS Code use, it
  just won't be tracked or pushed going forward.
- **Found, not fixed (low severity, out of scope):** `html5validator` (Nu
  HTML Checker) flagged pre-existing HTML structural issues — a duplicated
  `<head>` tag in `404.html`, and a few invalid nesting/attribute cases in
  `index.html` (around lines 97, 107, 118, 397, 1047). These are cosmetic
  markup issues with no functional or security impact; fixing them would
  mean editing a ~1400-line hand-maintained page and was judged out of
  scope for this pass. Flagging for a future, deliberate cleanup pass.

## Code Quality

No refactoring performed — the codebase is a hand-written single-page site
with no obvious dead code, duplicate logic, or unused dependencies beyond
what's noted above. No changes made beyond the fixes listed in this report.

## Testing

No automated test suite exists or is applicable (static site, no app logic).
Validation performed for this pass:

- Added `.github/scripts/check_local_links.py`, a regression check that
  parses `index.html`/`404.html` and verifies every local `src`/`href`
  reference resolves to a real file. Run locally — passes (2/2 files clean).
- Verified the jQuery 3.7.1 swap with a Node.js + jsdom smoke test: loaded
  `index.html`'s DOM, evaluated the new `jquery-3.7.1.min.js`, confirmed
  `jQuery.fn.jquery === "3.7.1"`, basic selector/constructor behavior, and
  a `.ready()` callback firing without errors.
- Full interactive browser verification (headless Chromium via Playwright)
  was attempted but the browser binary download was blocked by this
  session's network egress policy (same restriction class as the jQuery
  CDN). Recommend a manual once-over in a real browser after this lands,
  though the change is a same-API patch version with no reported breaking
  changes.

## CI/CD

No CI/CD previously existed (no `.github/workflows`). Added
`.github/workflows/html-check.yml`:

- Triggers on push to `main` and on pull requests.
- Runs `check_local_links.py` to catch broken internal asset/script/link
  references before they reach production.
- Deliberately does not run a strict HTML5 structural validator, since
  doing so would fail immediately on the pre-existing markup issues noted
  above (unrelated to any change in this pass) and isn't a useful blocking
  gate until those are addressed separately.
- Does not touch the GitHub Pages deployment mechanism (Pages continues to
  build directly from `main` via Jekyll, configured in repo Settings).

## Documentation

- Added `README.md` (none existed previously): project overview, tech
  stack, project structure, local development instructions, build/test
  notes, CI/CD description, and deployment/rollback process.

## Release Status

- Changes pushed to branch `claude/gallant-volta-ornnyr` and delivered via
  pull request rather than merged directly to `main` — this session's
  environment is configured to develop on that branch and always open a PR
  for review rather than push/merge directly to `main`.
- Deployment readiness: changes are low-risk (dependency patch bump,
  repository hygiene, new docs/CI) and do not alter site behavior or the
  deployment mechanism.
- Remaining actions: merge the PR when reviewed; optionally schedule a
  follow-up pass to clean up the pre-existing HTML structural issues noted
  above.
